import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#fff200',
      contrastText: '#000000',
    },
    secondary: {
      main: '#000000',
    },
  },
});

export default theme;
